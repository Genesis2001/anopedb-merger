<Window x:Class="MergeTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:MergeTool.ViewModels"
        mc:Ignorable="d"
        Title="AnopeDb Merge Tool"
		d:DataContext="{d:DesignInstance viewModels:MergerViewModel}"
		WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize"
		Width="960" Height="544">
	
	<Window.Resources>
		<Style TargetType="Button">
			<Setter Property="Margin" Value="1,0" />
			<Setter Property="Padding" Value="10,0" />
		</Style>
	</Window.Resources>
	
	<DockPanel>
		<StatusBar DockPanel.Dock="Bottom">
			<StatusBarItem>
				<Label Content="{Binding State, FallbackValue=Ready.}" />
			</StatusBarItem>
		</StatusBar>
		
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>
			
			<Grid.RowDefinitions>
				<RowDefinition Height="*" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>
			
			<Grid Grid.Row="0" Grid.Column="0" Margin="5">
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="*" />
				</Grid.RowDefinitions>
				
				<DockPanel Grid.Row="0">
					<Label DockPanel.Dock="Left" Content="Input" />
					<StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
						<Button Command="{Binding BrowseLeftInputCommand}" Content="Browse" />
						<Button Content="Load" Command="{Binding LoadLeftCommand}" />
					</StackPanel>
					<TextBox Text="{Binding LeftFileInput, UpdateSourceTrigger=PropertyChanged}" />
				</DockPanel>
				
				<TreeView Grid.Row="1" Margin="0,8">
				</TreeView>
			</Grid>

			<Grid Grid.Row="0" Grid.Column="1" Margin="5">
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="*" />
				</Grid.RowDefinitions>
				
				<DockPanel Grid.Row="0">
					<Label DockPanel.Dock="Left" Content="Input" />
					<StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
						<Button Command="{Binding BrowseRightInputCommand}" Content="Browse" />
						<Button Content="Load" Command="{Binding LoadRightCommand}" />
					</StackPanel>
					<TextBox Text="{Binding RightFileInput, UpdateSourceTrigger=PropertyChanged}" />
				</DockPanel>
				
				<TreeView Grid.Row="1" Margin="0,8">
				</TreeView>
			</Grid>
			
			<DockPanel Grid.Row="1" Grid.Column="1" Margin="10,5">
				<Label DockPanel.Dock="Left" Content="Output" />
				<StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
					<Button Content="Browse" Command="{Binding BrowseOutputFileCommand}" />
					<Button Content="Save" Command="{Binding SaveOutputCommand}" />
				</StackPanel>
				<TextBox Text="{Binding OutputFilePath, UpdateSourceTrigger=PropertyChanged}" Margin="2,0" />
			</DockPanel>
		</Grid>
	</DockPanel>
</Window>
